# Cloudflared

This is a sanitized example only. The current Hostinger inventory showed Nginx proxying `alexandria.grupototum.com` to `127.0.0.1:5000`; no Cloudflare Tunnel config is required for the current deployment.
